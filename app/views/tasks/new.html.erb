<h1> New Task </h1>

<%= form_for(@task) do |f| %>

	<div class='field'>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</div>

	<div class='actions'>
		<%= f.submit %>
	</div>
<% end %>

<%= link_to 'All Tasks', tasks_path %>
