<h1>Tasks</h1>

<ul>
	<% @tasks.each do |t| %>
	<li> <%= link_to t.title, t %> | <%= link_to 'Edit', edit_task_path(t) %> | <%=link_to 'Delete', t, method: :delete %> </li>
	<% end %>
</ul>

<%= link_to 'Add new task', new_task_path %>
